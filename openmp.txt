if(OPENMP_FOUND)
    include_directories(${OpenMP_INCLUDE_DIRS})
    target_link_libraries(main PRIVATE ${OpenMP_LIBRARIES})
    target_compile_options(main PRIVATE ${OpenMP_COMPILE_FLAGS} ${OpenMP_LINK_FLAGS})
else()
    message(WARNING "OpenMP not found.")
endif()
